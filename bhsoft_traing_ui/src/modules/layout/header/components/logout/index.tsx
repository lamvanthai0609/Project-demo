import React from 'react';
import Button from '../../../../components/button';

const Logout = () => {
     return (
          <div className="mx-2">
               <Button styles={'text-white bg-black px-5 py-3 rounded'} text="Logout" />
          </div>
     );
};

export default Logout;
